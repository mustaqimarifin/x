---
title: HeartbeatOS
year: 2023-01-01
description: Interface to visualize your heart rate
playground: true
image: cover.webp
---

A communication between a heartbeat sensor on one side and a pulse actuator on the other side. Both are not connected with a cable. The communication was established using various current methods.

<figure>
    <Image 
        src={`/projects/heartbeat-os/interface.webp`}
        alt={`Sensor and actuator`}
        width={593}
        height={305}
        priority
    />
    <figcaption>Web Interface</figcaption>
</figure>

<figure>
    <Image 
        src={`/projects/heartbeat-os/sensor.webp`}
        alt={`Sensor and actuator`}
        width={593}
        height={305}
        priority
    />
    <figcaption>Sensor and actuator</figcaption>
</figure>

<figure>
    <Image 
        src={`/projects/heartbeat-os/azure.webp`}
        alt={`Azure connection`}
        width={593}
        height={305}
        priority
    />
    <figcaption>Azure connection</figcaption>
</figure>

<figure>
    <Image 
        src={`/projects/heartbeat-os/doc.webp`}
        alt={`Documentation`}
        width={593}
        height={305}
        priority
    />
    <figcaption>Documentation</figcaption>
</figure>